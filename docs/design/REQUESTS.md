# Requests to ChatGPT

Newest at the top. Each request names what is wanted, the files it lands in,
and a version. Answer by updating those files on the `design` branch and
opening one pull request. Do not edit this file.

## R-002: Desktop HUD and home menu references, v2.1 (open)

Replace the visual spec with version 2.1. Remove every portrait, touch,
auto-throttle, safe-area and hydrant service-hold instruction. Keep and carry
forward: the Tailboard logo (blue hose letters TB, capital T, rounded B
corners, small red hydrant, no outlets on the B), palette, typeface, the world
art specs (engine, hydrant and hose, fire at three intensities, steam, house,
road and sidewalk colours, barricade and DEAD END sign, signal heads and stop
signs, cars), and the upgrade rules.

Rework for 1280x720 landscape, with a 960x540 variant of the HUD only:

- HUD: the active call is the loudest element during a call (fire health,
  time left, water) while the road ahead stays clear. Engine condition, call
  counter, credits and siren state are secondary. Off-screen call arrow at the
  screen edge. Prompt line for hydrant and suppression states, in words:
  "Slow down to hook up", "Hooking up", "Refilling, 64/100 units", "Tank full",
  "Hose snapped", "Knocking it down", "Refill needed", and the under-30-seconds
  state "Time left 0:28, running out", which changes weight or shape, not only
  colour.
- Minimap: bottom-right, about 200x150 px at 1280x720, road network on a dark
  translucent panel, engine as an oriented triangle, active call marker,
  hydrant dots, camera rectangle, and a small zoom control inside the panel
  that changes only the minimap.
- Home menu: logo and wordmark, "Start shift", "Data and credits", "Quit".

Files: `tailboard-visual-spec.md` (version 2.1 at the top),
`images/hud-desktop-v2.1.png`, `images/hud-narrow-v2.1.png`,
`images/home-menu-v2.1.png`. State px sizes, hex colours and anchors in the
spec for every element shown.

## R-001: Logo exports (open)

Commit the approved working logo to `images/`: `logo-icon-1024.png` on a plain
background, `logo-icon-180.png`, `logo-wordmark.png` (horizontal, for the home
menu), and `logo-mono.png` (single colour). Add a short logo section to the
spec: geometry of the mark in words, hex colours, and the typeface with its
licence.
